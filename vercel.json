{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/home", "destination": "/api/backend" },
    { "source": "/api/:path*", "destination": "/api/backend" }
  ]
}
